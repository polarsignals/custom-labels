[package]
name = "custom-labels"
version = "0.0.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[build-dependencies]
bindgen = "0.69"
cc = "1.0"

[dependencies]
libc = "0.2"
